const RepresentationOfProjects = ({img, head, para}) => {
    return(
        <div>
            <div>{img}</div>
            <div>{head}</div>
            <div>{para}</div>
        </div>
    )
}

export default RepresentationOfProjects;